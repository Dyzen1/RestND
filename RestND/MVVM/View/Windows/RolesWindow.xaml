<Window x:Class="RestND.MVVM.View.Windows.RolesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:RestND.MVVM.ViewModel"
        mc:Ignorable="d"
        Title="Roles"
        Width="650" Height="460"
        WindowStartupLocation="CenterOwner"
        Style="{StaticResource smallWindowStyle}"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.DataContext>
        <vm:RoleViewModel/>
    </Window.DataContext>

    <Border Style="{DynamicResource windowBorder}">
        <Grid Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock Text="Manage Roles"
                       Foreground="{DynamicResource LightText}"
                       FontWeight="Bold"
                       FontSize="16"
                       Margin="0,0,0,10"/>

            <!-- Create New Role -->
            <GroupBox Header="Create New Role" Grid.Row="1" Margin="0,0,0,10">
                <StackPanel Margin="8">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <Label Content="Role Name:" Width="110" VerticalAlignment="Center"/>
                        <TextBox Text="{Binding NewRoleName, UpdateSourceTrigger=PropertyChanged}"
                                 Width="260"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal">
                        <Label Content="Authorization:" Width="110" VerticalAlignment="Center"/>
                        <ComboBox ItemsSource="{Binding AuthorizationOptions}"
                                  SelectedItem="{Binding SelectedAuthorization, Mode=TwoWay}"
                                  Width="160"/>
                        <Button Content="Create"
                                Margin="10,0,0,0"
                                Style="{DynamicResource SubmitBtn}"
                                Command="{Binding AddRoleCommand}"
                                IsDefault="True"/>
                    </StackPanel>

                    <TextBlock Text="{Binding FormErrorMessage}"
                               Margin="0,8,0,0"
                               Foreground="{DynamicResource LightText}"
                               Opacity="0.8"/>
                </StackPanel>
            </GroupBox>

            <!-- Existing Roles -->
            <GroupBox Header="Existing Roles" Grid.Row="2">
                <DataGrid ItemsSource="{Binding Roles}"
                          SelectedItem="{Binding SelectedRole, Mode=TwoWay}"
                          AutoGenerateColumns="False"
                          HeadersVisibility="Column"
                          ColumnHeaderStyle="{StaticResource dataGridHeaderStyle}"
                          CanUserAddRows="False"
                          Margin="0">
                    <DataGrid.Columns>
                        
                        <DataGridTextColumn Header="Role Name"
                                            Binding="{Binding Role_Name, UpdateSourceTrigger=PropertyChanged}"
                                            Width="*"/>

                        <!-- Authorization column with enum dropdown -->
                        <DataGridTemplateColumn Header="Authorization" Width="160">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Role_Authorization}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                            <DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <ComboBox ItemsSource="{Binding DataContext.AuthorizationOptions, RelativeSource={RelativeSource AncestorType=Window}}"
                                              SelectedItem="{Binding Role_Authorization, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                              MinWidth="140"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellEditingTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>

            <!-- Footer actions -->
            <Grid Grid.Row="3" Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" Grid.Column="1" HorizontalAlignment="Right">
                    <Button Content="Delete"
                            Style="{DynamicResource SubmitBtn}"
                            Margin="0,0,8,0"
                            Command="{Binding DeleteRoleCommand}"/>
                    <Button Content="Update"
                            Style="{DynamicResource SubmitBtn}"
                            Margin="0,0,8,0"
                            Command="{Binding UpdateRoleCommand}"/>
                    <Button Content="Close"
                            Style="{DynamicResource SubmitBtn}"
                            Click="Cancel_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
