<Window x:Class="RestND.MVVM.View.DishWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:RestND.MVVM.ViewModel"
        xmlns:local="clr-namespace:RestND.MVVM.Model"
        xmlns:uc="clr-namespace:RestND.MVVM.View.UserControls" 
        xmlns:fa="http://schemas.fontawesome.com/icons/svg"
        mc:Ignorable="d"
        Style="{StaticResource windowStyle}"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Border BorderBrush="{DynamicResource LightText}" BorderThickness="3" CornerRadius="10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="450"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="100"/>
            </Grid.RowDefinitions>

            <Border Grid.Column="1" Grid.RowSpan="3"
                    VerticalAlignment="Stretch" BorderThickness="2"
                    BorderBrush="{DynamicResource secondaryHeaders}"/>


            <!-- Add New Dish Section -->
            <GroupBox Header="New Dish Details" Grid.Row="0" Grid.Column="0">
                <StackPanel Orientation="Vertical" Margin="0 10 0 0" HorizontalAlignment="Center">

                    <StackPanel Orientation="Horizontal" Margin="0 5 0 10">
                        <Label Content="Dish Name:"/>
                        <TextBox Text="{Binding NewDish.Dish_Name, UpdateSourceTrigger=PropertyChanged}"
                                 Margin="10 0 0 0"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0 5 0 10">
                        <Label Content="Dish Price:"/>
                        <TextBox Text="{Binding NewDish.Dish_Price, UpdateSourceTrigger=PropertyChanged}"
                                 Margin="10 0 0 0"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0 5 0 10">
                        <Label Content="Allergen Notes:" Width="120"/>
                        <ListBox ItemsSource="{Binding AllergenOptions}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox Content="{Binding Value}" 
                                              Foreground="{DynamicResource LightText}"
                                              IsChecked="{Binding IsSelected, Mode=TwoWay}"/>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0 5 0 10">
                        <Label Content="Dish Type:"/>
                        <ListBox ItemsSource="{Binding DishTypes}" 
                                SelectedItem="{Binding NewDish.Dish_Type, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                DisplayMemberPath="DishType_Name"
                                SelectionMode="Single" Margin="10 0 0 0"/>
                    </StackPanel>
                </StackPanel>
            </GroupBox>


            <GroupBox Header="Products For New Dish" Grid.Row="1" Grid.Column="0">
                <DataGrid ItemsSource="{Binding ProductSelections}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          HeadersVisibility="Column"
                          BorderThickness="0"
                          HorizontalAlignment="Stretch" Margin="0 2 0 0"
                          RowStyle="{DynamicResource rowStyle}"
                          CellStyle="{DynamicResource cellStyle}">
                    <DataGrid.Columns>
                        <DataGridCheckBoxColumn Width="30"
                            Binding="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                            <DataGridCheckBoxColumn.Header>
                                <fa:SvgAwesome Icon="Regular_SquareCheck" Width="15" Height="15"/>
                            </DataGridCheckBoxColumn.Header>
                        </DataGridCheckBoxColumn>

                        <!-- Name (read-only) -->
                        <DataGridTextColumn Header="Product"
                                Binding="{Binding Product_Name}"
                                IsReadOnly="True"
                                Width="*" />

                        <!-- Stock (read-only, optional but nice) -->
                        <DataGridTextColumn Header="Quantity In Stock"
                                Binding="{Binding Quantity_Available}"
                                IsReadOnly="True"
                                Width="*" />

                        <!-- Amount to use in this dish -->
                        <DataGridTextColumn Header="Amount (gr)"
                                Binding="{Binding AmountUsage, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>


            <Button Grid.Column="0" Grid.Row="2" Command="{Binding AddDishCommand}" Margin="0 15 0 0" 
                    Style="{DynamicResource addBtn}" HorizontalAlignment="Center" VerticalAlignment="Top">
                <StackPanel Orientation="Horizontal">
                    <fa:SvgAwesome Style="{DynamicResource addBtnIcon}"/>
                    <TextBlock Text="Add Dish" FontSize="13" Foreground="{DynamicResource LightText}" 
                                Margin="4,0,0,0"/>
                </StackPanel>
            </Button>

            <StackPanel Grid.Row="2" Grid.Column="0" Orientation="Horizontal"
                        Margin="0 0 0 25" HorizontalAlignment="Center" VerticalAlignment="Bottom">
                <Button Content="Update Dish" 
                         Click="UpdateDishBtn_Click"
                         Style="{DynamicResource SubmitBtn}"/>

                <Button Content="Delete Dish"
                        Command="{Binding DeleteDishCommand}"
                        Margin="20,0,0,0" Style="{DynamicResource SubmitBtn}"/>
            </StackPanel>

            <!-- Existing Dishes Grid -->
            <Border Grid.Column="1" Grid.RowSpan="3" Margin="8" 
                    Style="{DynamicResource windowBorder}">
                <DataGrid ItemsSource="{Binding Dishes}"
                          SelectedItem="{Binding SelectedDish}" 
                          CellStyle="{DynamicResource cellStyle}"
                          AutoGenerateColumns="False"
                          RowDetailsVisibilityMode="VisibleWhenSelected">

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Dish" Binding="{Binding Dish_Name}" Width="*"/>
                        <DataGridTextColumn Header="Price" Binding="{Binding Dish_Price}" Width="*" />
                        <DataGridTextColumn Header="Allergens" Binding="{Binding Allergen_Notes}" Width="*" />
                        <DataGridTextColumn Header="Dish Type" Binding="{Binding Dish_Type.DishType_Name}" Width="*" />
                    </DataGrid.Columns>

                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="DetailsVisibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="DetailsVisibility" Value="Visible"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>

                    <DataGrid.RowDetailsTemplate>
                        <DataTemplate>
                            <ItemsControl ItemsSource="{Binding ProductUsage}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <DockPanel>
                                            <TextBlock Text="{Binding Product_Name}" DockPanel.Dock="Left"/>
                                            <TextBlock Text="{Binding Amount_Usage, StringFormat={}{0} gr}" DockPanel.Dock="Right"/>
                                        </DockPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </DataTemplate>
                    </DataGrid.RowDetailsTemplate>

                </DataGrid>

            </Border>

            <uc:ReturnBtn x:Name="returnBtn" Grid.Row="2" Grid.Column="0"
                          VerticalAlignment="Bottom" Margin="15,0,0,15"/>
            <TextBlock Style="{DynamicResource copyWrite}" Grid.Row="2" Grid.Column="1" 
                       VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,15,20"/>
        </Grid>
    </Border>
</Window>
