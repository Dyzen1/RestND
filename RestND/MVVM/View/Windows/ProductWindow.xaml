<Window x:Class="RestND.MVVM.View.ProductWindow"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:viewModel="clr-namespace:RestND.MVVM.ViewModel"
      xmlns:fa="http://schemas.fontawesome.com/icons/svg"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="900"
      WindowState="Maximized"
      d:DataContext="{d:DesignInstance Type=viewModel:ProductViewModel, IsDesignTimeCreatable=True}">

    <Grid>
        <!-- Background Image -->
        <Image Style="{DynamicResource bg}" />

        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0 0 0 10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" Height="24" Background="{DynamicResource CardBG}"
                         Text="{Binding NewProduct.Product_Name, UpdateSourceTrigger=PropertyChanged}">
                    <TextBox.ToolTip>Product Name</TextBox.ToolTip>
                </TextBox>

                <TextBox Grid.Column="1" Margin="10,0,0,0" Height="24" Background="{DynamicResource CardBG}"
                         Text="{Binding NewProduct.Quantity_Available, UpdateSourceTrigger=PropertyChanged}">
                    <TextBox.ToolTip>Quantity Available</TextBox.ToolTip>
                </TextBox>

                <Button Grid.Column="2" Margin="10,0,0,0" Command="{Binding AddProductCommand}" Style="{DynamicResource addBtn}">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <fa:SvgAwesome Style="{DynamicResource addBtnIcon}"/>
                        <TextBlock Text="Add Product" FontSize="13" Foreground="{DynamicResource LightText}" Margin="4,0,0,0"/>
                    </StackPanel>
                </Button>
            </Grid>

            <DataGrid Grid.Row="1"
                      ItemsSource="{Binding Products}"
                      SelectedItem="{Binding SelectedProduct, Mode=TwoWay}"
                      AutoGenerateColumns="True"
                      Margin="0 0 0 10"
                      Background="{DynamicResource CardBG}"/>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="return" Grid.Column="0"
                        Style="{DynamicResource returnBtn}" Click="return_Click"
                        HorizontalAlignment="Left">
                    <fa:SvgAwesome Style="{DynamicResource returnBtnIcon}"/>
                </Button>

                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Style="{DynamicResource editBtn}">
                        <!-- Command="{Binding UpdateProductCommand}" -->
                        <fa:SvgAwesome Style="{DynamicResource editBtnIcon}"/>
                    </Button>
                    <Button Style="{DynamicResource deleteBtn}" Margin="5,0,0,0">
                        <!-- Command="{Binding DeleteProductCommand}" -->
                        <fa:SvgAwesome Style="{DynamicResource deleteBtnIcon}"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Window>
